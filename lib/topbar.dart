import 'package:flutter/material.dart';

PreferredSizeWidget TopBar() {
  return AppBar(
    backgroundColor: Colors.blue,
    elevation: 10,
    title: const Text('Time to practice: Flutter basics widget'),
  );
}
